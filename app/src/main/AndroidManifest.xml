<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.recyclertest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission
        android:name="android.permission.READ_PRIVILEGED_PHONE_STATE"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Recyclertest"
        android:usesCleartextTraffic="true">
        <activity android:name=".DailyReport" />
        <activity android:name=".Registration" />
        <activity android:name=".StockAdd" />
        <activity android:name=".subusercreate" />
        <activity android:name=".finalRecycer" />
        <activity android:name=".Getcontract" />
        <activity
            android:name=".home_page"
            android:label="@string/title_activity_home_page"
            android:theme="@style/Theme.Recyclertest.NoActionBar" />
        <activity android:name=".StockOutlist" />
        <activity android:name=".Volley_test" />
        <activity android:name=".Login" />
        <activity
            android:name=".MainActivity"
            android:label="Sells">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>